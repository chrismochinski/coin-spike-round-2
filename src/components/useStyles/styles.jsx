import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
    container: {
        backgroundColor: '#3BADD4',
        padding: '30px',
        color: 'white',
        fontFamily: 'Poppins',
    }
}))

export default useStyles;