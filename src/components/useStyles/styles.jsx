import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
    container: {
        backgroundColor: '#A200F5',
        padding: '30px',
        color: 'white',
        fontFamily: 'Poppins',
    }
}))

export default useStyles;